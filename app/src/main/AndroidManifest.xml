<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team.szkielet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".event.AddEvent"></activity>
        <activity android:name=".event.Events" />
        <activity android:name=".test.Json" />
        <activity android:name=".quiz.QuizMainActivity" />
        <activity android:name=".quiz.QuizActivity" />
        <activity
            android:name=".PopUpInPlany"
            android:theme="@style/AppTheme.PopUp" />
        <activity android:name=".Aktualnosci" />
        <activity android:name=".Prowadzacy" />
        <activity android:name=".Plany" />
        <activity
            android:name=".MainActivityBetter"
            android:theme="@style/Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>